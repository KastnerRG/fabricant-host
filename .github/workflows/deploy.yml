name: Deploy to Fabricant-Host

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:

    runs-on: self-hosted

    steps:
      - name: Run Ansible Playbook
        run: |
          pushd /home/fabricant-admin/fabricant-host
          git pull origin main
          source /home/fabricant-admin/fabricant-host/.venv/bin/activate
          ansible-playbook playbook.yaml
          popd
